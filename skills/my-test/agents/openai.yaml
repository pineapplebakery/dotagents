interface:
  display_name: "My Test"
  short_description: "Pytest TDD, test execution, and scoped verification"
  default_prompt: "Use $my-test to change code with pytest TDD or to run and analyze tests without edits."
