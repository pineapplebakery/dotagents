interface:
  display_name: "My Test"
  short_description: "Test-only changes, pytest checks, and delegated TDD"
  default_prompt: "Use $my-test for test-only changes or to run and analyze the relevant pytest checks without editing production code."
