interface:
  display_name: "My Test"
  short_description: "Test-first development and staged verification with pytest"
  default_prompt: "Use $my-test to write pytest tests before changing production code."
